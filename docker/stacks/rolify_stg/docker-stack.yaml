services:
  web:
    image: hub.ar5.in/rolify/web:${IMAGE_TAG}
    volumes:
      - rolify-storage:/var/www/storage:ro
      - rolify-public-assets:/var/www/public/build:ro
    networks:
      - traefik_traefik_proxy
      - rolify
    depends_on:
      - app
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.rolify-stg.rule=Host(`stg-rolify.ar5.in`)"
        - "traefik.http.routers.rolify-stg.entrypoints=websecure"
        - "traefik.http.routers.rolify-stg.tls.certresolver=letsencrypt"
        - "traefik.http.services.rolify-stg.loadbalancer.server.port=80"
        - "traefik.http.middlewares.https-redirect.headers.customrequestheaders.X-Forwarded-Proto=https"

  app:
    image: hub.ar5.in/rolify/app:${IMAGE_TAG}
    volumes:
      - rolify-public-assets:/var/www/public/build
      - rolify-storage:/var/www/storage
    env_file:
      - .env
    secrets:
      - rolify_app_key
      - rolify_postgres_db
      - rolify_postgres_user
      - rolify_postgres_password
      - rolify_stg_plunk_api_key
    environment:
      - APP_KEY_FILE=/run/secrets/rolify_app_key
      - DB_DATABASE_FILE=/run/secrets/rolify_postgres_db
      - DB_USERNAME_FILE=/run/secrets/rolify_postgres_user
      - DB_PASSWORD_FILE=/run/secrets/rolify_postgres_password
      - PLUNK_API_KEY_FILE=/run/secrets/rolify_stg_plunk_api_key
    networks:
      - rolify
    healthcheck:
      test: ["CMD-SHELL", "php-fpm-healthcheck || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
    depends_on:
      - postgres
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    # entrypoint: ["/bin/sh", "-c", "export DB_PASSWORD=$(cat /run/secrets/db_password) && exec php-fpm"]

  scheduler:
    image: hub.ar5.in/rolify/app:${IMAGE_TAG}
    volumes:
      - rolify-public-assets:/var/www/public/build
      - rolify-storage:/var/www/storage
    env_file:
      - .env
    secrets:
      - rolify_app_key
      - rolify_postgres_db
      - rolify_postgres_user
      - rolify_postgres_password
      - rolify_stg_plunk_api_key
    environment:
      - APP_KEY_FILE=/run/secrets/rolify_app_key
      - DB_DATABASE_FILE=/run/secrets/rolify_postgres_db
      - DB_USERNAME_FILE=/run/secrets/rolify_postgres_user
      - DB_PASSWORD_FILE=/run/secrets/rolify_postgres_password
      - PLUNK_API_KEY_FILE=/run/secrets/rolify_stg_plunk_api_key
      - CONTAINER_ROLE=scheduler
    networks:
      - rolify
    depends_on:
      - postgres
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  queue:
    image: hub.ar5.in/rolify/app:${IMAGE_TAG}
    volumes:
      - rolify-public-assets:/var/www/public/build
      - rolify-storage:/var/www/storage
    env_file:
      - .env
    secrets:
      - rolify_app_key
      - rolify_postgres_db
      - rolify_postgres_user
      - rolify_postgres_password
      - rolify_stg_plunk_api_key
    environment:
      - APP_KEY_FILE=/run/secrets/rolify_app_key
      - DB_DATABASE_FILE=/run/secrets/rolify_postgres_db
      - DB_USERNAME_FILE=/run/secrets/rolify_postgres_user
      - DB_PASSWORD_FILE=/run/secrets/rolify_postgres_password
      - PLUNK_API_KEY_FILE=/run/secrets/rolify_stg_plunk_api_key
      - CONTAINER_ROLE=queue
    networks:
      - rolify
    depends_on:
      - postgres
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  postgres:
    image: postgres:16
    user: postgres
    secrets:
      - rolify_postgres_db
      - rolify_postgres_user
      - rolify_postgres_password
    environment:
      - POSTGRES_DB_FILE=/run/secrets/rolify_postgres_db
      - POSTGRES_USER_FILE=/run/secrets/rolify_postgres_user
      - POSTGRES_PASSWORD_FILE=/run/secrets/rolify_postgres_password
    volumes:
      - rolify-postgres:/var/lib/postgresql/data
    networks:
      - rolify
    healthcheck:
      test: [ "CMD", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  redis:
    image: redis:alpine
    networks:
      - rolify
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

networks:
  rolify:
    driver: overlay

  traefik_traefik_proxy:
    external: true

volumes:
  rolify-postgres:
  rolify-storage:
  rolify-public-assets:

secrets:
  rolify_app_key:
    external: true
  rolify_postgres_db:
    external: true
  rolify_postgres_user:
    external: true
  rolify_postgres_password:
    external: true
  rolify_stg_plunk_api_key:
    external: true

